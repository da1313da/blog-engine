spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog-engine
    username: root
    password: mysql
  jpa:
    generate-dll: flase
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true